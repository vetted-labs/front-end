/**
 * Sentinel id used by the endorsement guild switcher to represent the
 * cross-guild ("All guilds") scope. Mirrors the `"all"` convention already
 * used by the applications screen (`ApplicationsPage`/`useApplicationsData`).
 */
export const ALL_GUILDS_ID = "all";
